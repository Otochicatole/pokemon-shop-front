'use client';
import Link from 'next/link';
import { useSearchParams } from 'next/navigation';
import { useState } from 'react';
import { toast } from 'sonner';
import { forgotPassword, resetPassword, verifyEmail } from '../infrastructure/api';
import { Button } from '@/components/button';
export function TokenForm({ mode }: { mode: 'verify' | 'forgot' | 'reset' }) { const params = useSearchParams(); const [token, setToken] = useState(params.get('token') ?? ''); const [email, setEmail] = useState(params.get('email') ?? ''); const [password, setPassword] = useState(''); const [busy, setBusy] = useState(false); const [done, setDone] = useState(false); const submit = async (e: React.FormEvent) => { e.preventDefault(); setBusy(true); try { if (mode === 'verify') await verifyEmail(token); else if (mode === 'forgot') await forgotPassword(email); else await resetPassword(token, password); setDone(true); toast.success(mode === 'verify' ? 'Email verificado' : 'Listo'); } catch (err) { toast.error(err instanceof Error ? err.message : 'No pudimos completar la operación'); } finally { setBusy(false); } }; if (done) return <div className="success-message"><span>✓</span><h2>{mode === 'forgot' ? 'Revisá tu correo' : mode === 'verify' ? 'Cuenta verificada' : 'Contraseña actualizada'}</h2><p>{mode === 'forgot' ? 'Si existe una cuenta con ese email, vas a recibir instrucciones.' : 'Ya podés continuar con tu cuenta.'}</p><Link href={mode === 'forgot' || mode === 'reset' ? '/auth/login' : '/'} className="button button-primary">Continuar</Link></div>; return <form className="auth-form" onSubmit={submit}>{mode === 'forgot' ? <label>Email<input required type="email" value={email} onChange={(e) => setEmail(e.target.value)} /></label> : <label>Token<input required value={token} onChange={(e) => setToken(e.target.value)} placeholder="Pegá el token recibido" /></label>}{mode === 'reset' && <label>Nueva contraseña<input required minLength={12} type="password" value={password} onChange={(e) => setPassword(e.target.value)} /></label>}<Button disabled={busy}>{busy ? 'Enviando…' : mode === 'forgot' ? 'Enviar instrucciones' : mode === 'verify' ? 'Verificar email' : 'Cambiar contraseña'}</Button></form>; }
