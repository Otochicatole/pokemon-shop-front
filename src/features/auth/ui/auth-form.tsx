'use client';
import Link from 'next/link';
import { useRouter, useSearchParams } from 'next/navigation';
import { useState } from 'react';
import { login, register } from '../infrastructure/api';
import { Button } from '@/shared/ui/button';
export function AuthForm({ mode }: { mode: 'login' | 'register' }) { const router = useRouter(); const params = useSearchParams(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [name, setName] = useState(''); const [busy, setBusy] = useState(false); const [error, setError] = useState(''); const submit = async (e: React.FormEvent) => { e.preventDefault(); setBusy(true); setError(''); try { if (mode === 'register') { await register(email, password, name); router.push(`/auth/verify-email?pending=1&email=${encodeURIComponent(email)}`); } else { await login(email, password); const returnTo = params.get('returnTo'); router.push(returnTo && returnTo.startsWith('/') && !returnTo.startsWith('//') ? returnTo : '/account'); } } catch (err) { setError(err instanceof Error ? err.message : 'No pudimos completar la operación'); } finally { setBusy(false); } }; return <form className="auth-form" onSubmit={submit}>{mode === 'register' && <label>Nombre<input value={name} onChange={(e) => setName(e.target.value)} autoComplete="name" placeholder="Tu nombre" /></label>}<label>Email<input required type="email" value={email} onChange={(e) => setEmail(e.target.value)} autoComplete="email" placeholder="vos@ejemplo.com" /></label><label>Contraseña<input required type="password" minLength={12} value={password} onChange={(e) => setPassword(e.target.value)} autoComplete={mode === 'login' ? 'current-password' : 'new-password'} placeholder="12 caracteres o más" /></label>{error && <p className="form-error" role="alert">{error}</p>}<Button disabled={busy}>{busy ? 'Un momento…' : mode === 'login' ? 'Ingresar' : 'Crear cuenta'}</Button>{mode === 'login' && <><a className="google-button" href="/api/v1/auth/google">Continuar con Google</a><Link href="/auth/forgot-password" className="form-link">¿Olvidaste tu contraseña?</Link></>}{mode === 'register' && <p className="form-hint">Te enviaremos un token para verificar tu email antes de comprar.</p>}</form>; }
